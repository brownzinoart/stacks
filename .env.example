# ============================================
# Stacks App - Environment Variables
# ============================================
# Copy this file to .env.local and fill in your API keys
# .env.local is git-ignored and will not be committed

# ============================================
# REQUIRED API Keys (Server-side)
# ============================================

# Anthropic Claude API Key (REQUIRED)
# Used for: Natural language book search and query enrichment
# Get from: https://console.anthropic.com/
# Cost: ~$0.001-0.003 per search with Haiku model
# Rate limit: Pay-as-you-go
ANTHROPIC_API_KEY=

# ============================================
# OPTIONAL API Keys (Server-side)
# ============================================

# TMDB API Key (Optional but recommended)
# Used for: Extracting themes from movie references in search queries
# Get from: https://www.themoviedb.org/signup → Settings > API
# Rate limit: 40 requests/10 seconds (free tier)
# Note: App will gracefully degrade if not set
TMDB_API_KEY=

# Google Books API Key (Optional - server-side)
# Used for: Book metadata enrichment (server-side operations)
# Get from: https://console.cloud.google.com/apis/library/books.googleapis.com
# Rate limit: 1000 requests/day (without key), 100,000/day (with key)
# Note: App works without this key but with lower rate limits
GOOGLE_BOOKS_API_KEY=

# ============================================
# OPTIONAL API Keys (Client-side - NEXT_PUBLIC_ prefix)
# ============================================
# WARNING: NEXT_PUBLIC_ variables are exposed to the browser
# Only use non-sensitive keys or keys with restricted scopes

# Google Books API Key (Client-side)
# Used for: Book cover images and ratings in browser
# Get from: https://console.cloud.google.com/apis/library/books.googleapis.com
# Rate limit: 1000 requests/day (without key), 100,000/day (with key)
# Note: Can use same key as server-side GOOGLE_BOOKS_API_KEY
NEXT_PUBLIC_GOOGLE_BOOKS_API_KEY=

# New York Times Bestseller API Key (Optional)
# Used for: Checking if books are on bestseller lists
# Get from: https://developer.nytimes.com/
# Rate limit: 500 requests/day, 5 per minute
# Note: App will gracefully degrade if not set
NEXT_PUBLIC_NYT_API_KEY=

# ============================================
# Development/Testing
# ============================================

# Node Environment (automatically set by Next.js)
# Options: development, production, test
# NODE_ENV=development
